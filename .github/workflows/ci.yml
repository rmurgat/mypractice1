name: lo-que-queramos
on:
  push:
    branches: [ main ]

jobs:
  platzi:
    runs-on: ubuntu-latest
    steps:
      - name: 'Hello World'
        run: echo Hello, world!
      - name: Dime la fecha
        run: date
      - name: Muestra el contenido del direcotrio
        run: ls -la